import Enum from "@/js/enum/enum";
export default {
  BasicFeature: [
    Enum.KeyCode.Backspace,
    Enum.KeyCode.ArrowLeft,
    Enum.KeyCode.ArrowRight,
    Enum.KeyCode.Delete,
    Enum.KeyCode.TAB,
  ],
};
